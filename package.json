{
  "name": "jarvis-control-transport",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public, non-executable transport for bounded Jarvis Host Ops command envelopes and their sanitized public receipt journal.",
  "scripts": {
    "check": "node --check receipts/validate-receipt.mjs && node --check receipts/test/validate-receipt.test.mjs",
    "test": "node --test --test-reporter=spec \"receipts/test/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
